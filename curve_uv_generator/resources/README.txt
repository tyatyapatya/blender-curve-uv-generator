Place your authored node-group resource file here:

  gn_assets.blend

Requirements:
- Include a node group named exactly: Curve_UV.002
- This add-on appends that node group at runtime (it does not rebuild nodes in Python).
